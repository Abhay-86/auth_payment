## 🚀 Full-Stack Auth & Payments Platform

A production-ready starter kit featuring **Django + Next.js** with Google OAuth, email/OTP authentication, role-based access control, and Stripe-ready payment APIs. Includes modular backend apps, a modern frontend UI, and Docker-based local development.

### 🔑 What’s Inside

* **Google OAuth** login & account linking
* **Email verification + OTP** authentication
* **Role & feature-based access control**
* **Razorpay-ready** payment endpoints & models
* **Modular Django apps**: accounts, payments, features
* **Next.js frontend** with reusable auth UI + protected routes
* **Docker-compose storage** for Postgres and pgAdmin

### 🧱 Tech Stack

* **Backend:** Django, DRF
* **Frontend:** Next.js (React + TypeScript)
* **Database:** PostgreSQL
* **Payments:** Razorpay (optional, pluggable)

### ⚡ Quick Start (Dev)

* Backend: install requirements → set env → migrate → runserver
* Frontend: install deps → set env → `npm run dev`

Perfect foundation for SaaS products, dashboards, or any app needing secure authentication, feature gating, and payment flows.
